<h1>Lists</h1>
<ul>
  <% @lists.each do |list| %>
    <li>
      <%= list.name %> <%= link_to "See the details", list_path(list) %>
    </li>
  <% end %>
</ul>
<%= link_to "Add a new list", new_list_path %>
